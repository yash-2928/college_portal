import { CardActions, IconButton } from "@material-ui/core";
import { CloudDownload, Comment, Favorite, Report } from "@material-ui/icons";
import React, { useState } from "react";
import { Card, Col, Container } from "react-bootstrap";
import CommentItem from "./CommentItem";




export default function Postitem(props) {

  return (
    <Container fixed>
      <Card>
        <Card.Header>
          <Col>
            <Card.Img src={props.fileUrl} roundedCircle />
            <Card.Text>
              {props.firstname}
              {props.lastname}
            </Card.Text>
            <Card.Date>{props.postDate}</Card.Date>
          </Col>
        </Card.Header>
        <Card.Body>
          <Card.Text>
            {props.postTitle}
            {props.contant}
          </Card.Text>
          {props.postType?.startsWith('image') && <Card.Img variant="bottom" src={props.fileUrl} />}
          {props.postType && props.postType === "application/pdf" && <div>pdf</div>}
        </Card.Body>
        {/* <Card.Footer>
          <CardActions disableSpacing>
            <IconButton aria-label="add to favorites">
              <Favorite />
            </IconButton>
            <IconButton aria-label="download">
              <CloudDownload />
            </IconButton>
            <IconButton onclick={comment} aria-label="comment">
              <Comment />
            </IconButton>
            <IconButton onclick={report} aria-label="report">
              <Report />
            </IconButton>
          </CardActions>
        </Card.Footer> */}
      </Card>
    </Container>
  );
}

/*<div>
    <h1>{props.postTitle}</h1>
    <h3>{props.postDate}</h3>
    <p>{props.content}</p>
    <hr />
    <div style={{ padding: "0px 50px" }}>
        {props.comments?.map((comment, i) => <CommentItem key={i} {...comment} />)}
    </div>
</div>*/
